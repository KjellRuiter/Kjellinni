<!DOCTYPE html>
<html lang="en">
<head>
    <%- include ('../partials/head') %>
    <link rel="stylesheet" href="/css/chat.css">
</head>

<body>
    
    <div class="chat-area">
        <div class="chat-title">
          <a href="/matches" class="back-button"></a>
          <img
            class="match-image chat-img"
            src="https://cdn130.picsart.com/274506323021201.jpg?r1024x1024"
            alt=""
          />
          <h3 class="match-name chat-name">A girls name</h3>
          <button class="unmatch-heart">💔</button>
          <div class="unmatch-question hidden">
            <p>Are you sure you want to unmatch?</p>
            <div>
              <button class="yea-unmatch">yes</button>
              <button class="dont-unmatch">no</button>
            </div>
          </div>
        </div>
        <ul class="chat-container">
            <% if (chatHistory) { chatHistory.forEach(function(chat) { %>
              <li class="message" data-sender="<%=chat.from%>"><p><%=chat.msg%></p><span><%=chat.time%></span></li>
              <% })} %>
              <% if (1 > chatHistory.length) { %>
                <li class="prompt">
                  <p>Send a message and she might actually reply</p>
                </li>
              <% } %>
        </ul>
        <% if (roomID) { %>
          <form action="/chat/<%=roomID%>" method="post" class="input-container">
            <input id="textMessage" class="textMessage" type="text" name="textMessage" autocomplete="off" required="required" />
            <button type="submit" class="send">send</button>
          </form>
          <% } %>
        <!-- <div class="input-container">
          <input class="input" type="text" placeholder="send a message" />
          <button class="send">send</button>
        </div> -->
      </div>
      <p class="unmatch-popup hidden">
        you have unmatched <span class="unmatched-person">Baby</span>
      </p>

</body>

</html>